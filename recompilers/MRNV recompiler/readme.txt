About:
This recompiler reads in raw* csv outputs from MR studies in R.

Instruction:
1) Add CSV R output to this directory named 'r_output.csv'. 
2) Run csvToJson.py.
3) Run recompiler.py.

*Where exposure/outcome names include special characters (e.g., ',' or '|') which force line breaks, manual re-tabling is required.